{
  "modules": ["lib", ["mod1", "mod2", {
    "name": "mod3",
    "dependencies": [],
    "builder": {
      "command": "node ${modulePath}/builder.js"
    }
  }]],
  "dependencies": ["lib"],
  "assets": "dist/assets.json",
  "parallel": 3,
  "force": false,
  "builder": {
    "command": "webpack -p --color --config ${modulePath}/webpack.config.js",
    "options": {
      "cwd": "${modulePath}",
      "env": {
        "MODULE_NAME": "${moduleName}",
        "MODULE_PATH": "${modulePath}"
      },
      "timeout": 0
    }
  }
}